<Window x:Class="TEXTA_web.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TEXTA_web.ViewModels"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Icon="/Assets/IconWeb.ico"
        Title="TEXTA web"
        Height="600"
        Width="1000"
        AllowDrop="True">

    <!-- DataContext MVVM -->
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- ============================= -->
        <!-- BARRE SUPÉRIEURE -->
        <!-- ============================= -->
        <Grid Grid.Row="0" Background="#F5F5F5">

            <!-- Gauche -->
            <StackPanel Orientation="Horizontal" Margin="5,0">

                <Button Content="☰"
                        Width="50"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Command="{Binding NewTabCommand}"/>

                <Button Content="⟳"
                        Width="50"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Command="{Binding RefreshCommand}"/>

            </StackPanel>

            <!-- Centre -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <TextBox Width="450"
                         Padding="4"
                         Text="{Binding AddressBar, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"/>

                <Button Content="Go"
                        Margin="5,0,0,0"
                        Command="{Binding NavigateCommand}"/>

            </StackPanel>

            <!-- Droite -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,10,0">

                <Button Content="Tool"
                        Width="60"
                        Background="Transparent"
                        BorderBrush="Transparent"/>
            </StackPanel>

        </Grid>

        <!-- ============================= -->
        <!-- TABCONTROL MVVM -->
        <!-- ============================= -->

        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    TabStripPlacement="Left">

            <!-- Header Template -->
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}"
                                   Margin="0,0,5,0"/>

                        <Button Content="x"
                                Width="18"
                                Height="18"
                                Padding="0"
                                Command="{Binding DataContext.CloseTabCommand,
                                          RelativeSource={RelativeSource AncestorType=TabControl}}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <!-- Content Template -->
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <wv2:WebView2 Source="{Binding Url}" />
                </DataTemplate>
            </TabControl.ContentTemplate>

        </TabControl>

    </Grid>
</Window>
